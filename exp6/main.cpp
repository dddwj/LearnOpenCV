#include <iostream>
/*
 * 1. 对实验图像1执行膨胀处理（2次膨胀，采用矩形结构元）。要求创建一个可以调节结构元半径r的进度条，比较不同半径时膨胀结果的不同。
 * 2. 利用OpenCV提供的Sobel()函数（梯度算子尺寸是3×3），输出实验图像3的水平梯度图、垂直梯度图、梯度强度图（边缘强度以平方根形式来衡量）以及梯度强度图的反色图。

 */

using namespace std;
int main(int argc, const char * argv[]) {
    // insert code here...
    // argv[0] 存放的是完整程序 argv[1]存放命令行参数

    return 0;
}

